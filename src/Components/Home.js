import React from "react";
import styled from "styled-components";

const StyledDiv = styled.div``;

const Home = () => {
  return (
    <StyledDiv>
      <h1>Home Section</h1>
    </StyledDiv>
  );
};

export default Home;
